# Production dependencies untuk backend Flask + LSTM
# Install: pip install -r requirements-prod.txt

# Deep Learning (CPU-only — lebih kecil ~200MB dibanding tensorflow biasa)
tensorflow-cpu==2.15.0

# Machine Learning
scikit-learn==1.3.0

# Data manipulation
numpy==1.24.3
pandas==2.0.3

# Solar position & irradiance (used by plts_physics.py)
pvlib==0.10.5

# HTTP & Web server
requests==2.31.0
flask==2.3.3
flask-cors==4.0.0
gunicorn==21.2.0

# Time / Timezone
python-dateutil==2.8.2
pytz==2023.3
